# Copy this file to .env in the same folder (Frontend/.env).
# Point this at wherever the backend actually runs — localhost while
# you're developing, your server's real address once it's deployed.
SENTINEL_API_URL=http://127.0.0.1:8000
